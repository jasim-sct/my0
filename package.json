{
  "name": "websites",
  "private": true,
  "description": "Repo root — Vercel deploys only nextjs-website (see vercel.json rootDirectory)",
  "packageManager": "pnpm@9.15.0",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "dev": "pnpm --dir nextjs-website dev",
    "build": "pnpm --dir nextjs-website build",
    "start": "pnpm --dir nextjs-website start",
    "lint": "pnpm --dir nextjs-website lint"
  }
}
